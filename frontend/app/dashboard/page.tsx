import { ComingSoon } from "@/components/dashboard/coming-soon";

export default function OverviewPage() {
  return (
    <ComingSoon
      title="Overview"
      description="KPI tiles, spending trend, top vendors, spending by category, recent uploads, and pending questions."
    />
  );
}
