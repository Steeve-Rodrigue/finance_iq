.next
node_modules
package-lock.json

# Pre-existing static prototype and docs, not part of the Next.js app
clarify.html
CLAUDE.md

# shadcn/ui-generated components: regenerated by the shadcn CLI, kept in its
# own style so re-running `shadcn add` doesn't create formatting-only diffs
components/ui
lib/utils.ts
